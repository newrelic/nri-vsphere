builds:
  - id: nri-vsphere-nix
    main: ./src
    binary: nri-vsphere
    gobinary: "go1.9.7"
    ldflags:
      - -s -w -X main.integrationVersion={{.Version}} -X main.gitCommit={{.Commit}} -X main.buildDate={{.Date}}
    goos:
      - linux
      - darwin
    goarch:
      - 386
      - amd64
      - arm
      - arm64
    ignore:
      - goos: darwin
        goarch: 386

  - id: nri-vsphere-win
    main: ./src
    binary: nri-vsphere
    gobinary: "go1.9.7"
    ldflags:
      - -s -w -X main.integrationVersion={{.Version}} -X main.gitCommit={{.Commit}} -X main.buildDate={{.Date}}
    goos:
      - windows
    goarch:
      - 386
      - amd64
    hooks:
      pre: build/windows/set_exe_properties.sh {{ .Env.TAG }} "vsphere"

nfpms:
  - id: nri-vsphere-deb
    builds:
      - nri-vsphere-nix
    file_name_template: "{{ .ProjectName }}_{{ .Version }}-1_{{ .Arch }}"
    vendor: "New Relic, Inc."
    homepage: "https://www.newrelic.com/infrastructure"
    maintainer: "New Relic Infrastructure Team <infrastructure-eng@newrelic.com>"
    description: "New Relic Infrastructure vsphere Integration extend the core New Relic\nInfrastructure agent's capabilities to allow you to collect metric and\nlive state data from vsphere components."
    license: "https://newrelic.com/terms (also see LICENSE installed with this package)"
    # Formats to be generated.
    formats:
      - deb
    bindir: "/var/db/newrelic-infra/newrelic-integrations/bin"
    files:
      "CHANGELOG.md": "/usr/share/doc/nri-vsphere/CHANGELOG.md"
      "README.md":  "/usr/share/doc/nri-vsphere/README.md"
      "LICENSE": "/usr/share/doc/nri-vsphere/LICENSE"
    config_files:
      "vsphere-definition.yml": "/var/db/newrelic-infra/newrelic-integrations/vsphere-definition.yml"
      "vsphere-config.yml.sample": "/etc/newrelic-infra/integrations.d/vsphere-config.yml.sample"
    dependencies:
      - "newrelic-infra"

  - id: nri-vsphere-rpm
    builds:
      - nri-vsphere-nix
    file_name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Arch }}"
    vendor: "New Relic, Inc."
    homepage: "https://www.newrelic.com/infrastructure"
    maintainer: "New Relic Infrastructure Team <infrastructure-eng@newrelic.com>"
    description: "New Relic Infrastructure vsphere Integration extend the core New Relic\nInfrastructure agent's capabilities to allow you to collect metric and\nlive state data from vsphere components."
    license: "https://newrelic.com/terms (also see LICENSE installed with this package)"
    # Formats to be generated.
    formats:
      - rpm
    bindir: "/var/db/newrelic-infra/newrelic-integrations/bin"
    files:
      "CHANGELOG.md": "/usr/share/doc/nri-vsphere/CHANGELOG.md"
      "README.md":  "/usr/share/doc/nri-vsphere/README.md"
      "LICENSE": "/usr/share/doc/nri-vsphere/LICENSE"
    config_files:
      "vsphere-definition.yml": "/var/db/newrelic-infra/newrelic-integrations/vsphere-definition.yml"
      "vsphere-config.yml.sample": "/etc/newrelic-infra/integrations.d/vsphere-config.yml.sample"
    dependencies:
      - "newrelic-infra"
    overrides:
      rpm:
        replacements:
          amd64: 1.x86_64

archives:
  - id: nri-vsphere-nix
    builds:
      - nri-vsphere-nix
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Version }}_{{ .Arch }}_dirty"
    files:
      - vsphere-config.yml.sample
      - vsphere-definition.yml
    format: tar.gz

  - id: nri-vsphere-win
    builds:
      - nri-vsphere-win
    name_template: "{{ .ProjectName }}-{{ .Arch }}.{{ .Version }}_dirty"
    files:
      - vsphere-win-config.yml.sample
      - vsphere-win-definition.yml
    format: zip

# we use custom publisher for fixing archives and signing them
release:
  disable: true